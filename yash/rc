##### Sample Yashrc #####
# This is a sample initialization script for yash. Copy this file to ~/.yashrc
# and add your favorite customization to it.


# Firstly, load the common customization script.
# If you don't like settings applied in this script, remove this line.
. --autoload --no-alias initialization/common

# These are additional aliases that are not defined in the common script.
# Uncomment to enable them.
#alias g='grep'
#alias l='$PAGER'
#alias --global L='|$PAGER'
#alias --global N='>/dev/null 2>&1' N1='>/dev/null' N2='2>/dev/null'

# Uncomment if you want to clear the screen with Ctrl-L.
#bindkey --emacs '\^L' clear-and-redraw-all
#bindkey --vi-insert '\^L' clear-and-redraw-all
#bindkey --vi-command '\^L' clear-and-redraw-all

# Uncomment to enable direnv support. (jq required)
#_update_direnv() {
#  eval "$(
#    direnv export json |
#    jq -r 'to_entries | .[] |
#    if .value == null then
#      @sh "unset \(.key)"
#    else
#      @sh "export \(.key)=\(.value)"
#    end'
#  )"
#}
#_update_direnv
#YASH_AFTER_CD=("$YASH_AFTER_CD" '_update_direnv')

# And add your own customization below.

export XDG_CONFIG_HOME="$HOME/.config"
HX=/home/apd/local/bin/hx

alias em="$HOME/local/bin/emacs-nw"
alias vimrc="$HX $HOME/.config/nvim/init.vim"
alias brc="$HX $HOME/.bashrc"
alias brofile="$HX $HOME/.bash_profile"
alias profile="$HX $HOME/.profile"
alias rebash=". $HOME/.bash_profile"
alias barebash="bash --norc"
alias zconf="$HX $HOME/.config/zellij/config.kdl"
alias zlay="$HX $HOME/.config/zellij/layouts/default.kdl"
alias wconf="$HX $HOME/.config/wezterm/wezterm.lua"
alias econf="$HX $HOME/.config/emacs/init.el"
alias nuconf="$HX $HOME/.config/nushell/config.nu"
alias nuenv="$HX $HOME/.config/nushell/env.nu"
alias kconf="$HX $HOME/.config/kitty/kitty.conf"
alias sconf="$HX $HOME/.config/sway/config"
alias barconf="$HX $HOME/.config/waybar/config.jsonc"

alias gs="git status"
alias gco="git checkout"
alias gsw="git switch"

# bash specific
# test -r "$HOME/.fzf.bash" && . "$HOME/.fzf.bash"
# test -r "$HOME/.local/bin/mise" && eval "$($HOME/.local/bin/mise activate bash)"


# function y() {
# 	local tmp="$(mktemp -t "yazi-cwd.XXXXXX")" cwd
# 	yazi "$@" --cwd-file="$tmp"
# 	if cwd="$(command cat -- "$tmp")" && [ -n "$cwd" ] && [ "$cwd" != "$PWD" ]; then
# 		builtin cd -- "$cwd"
# 	fi
# 	rm -f -- "$tmp"
# }

# vim: set et sw=2 sts=2 tw=78 ft=sh:

eval "$((starship init bash))"
